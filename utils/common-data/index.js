const language = ['en','vi'];

const roleList = ['admin', 'nvpv', 'nvpc'];

const dishStatus = ['active', 'inactive'];

const tableStatus = ['empty', 'full', 'order', 'notUse'];
export default {
  language,
  roleList,
  dishStatus,
  tableStatus
};
